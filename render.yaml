services:
  - type: web
    name: bank-marketing-dashboard
    env: python
    buildCommand: pip install -r requirements-pythonanywhere.txt
    startCommand: gunicorn dashboard.app:server --workers 1 --timeout 120
    plan: free
